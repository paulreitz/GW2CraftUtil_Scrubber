/*
In the related projects this will, of course, be set in environment variables.
This was not necessary for this, since it will only be run locally and pushed to the live server 
from the dev environment.
*/

export default {
    server: 'localhost',
    database: 'GW2Craft',
    user: 'DevUser1',
    password: '$uperM4n'
}